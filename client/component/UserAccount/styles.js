import { StyleSheet, Dimensions } from 'react-native';

export default StyleSheet.create({
  logo: {
    height: 240,
    width: 180,
    alignSelf: 'center',
    margin: 30,
  },
  logout: {
    position: 'absolute',
    bottom: 0,
    alignSelf: 'center',
  },
});
